[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck - this is a background scheduler, not a web service
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
